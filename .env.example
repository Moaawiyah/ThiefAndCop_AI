# Copy to .env and fill in real values. Never commit the real .env file.

# GLM (z.ai) cloud LLM API key — preferred over config.yaml's llm.api_key.
# Read by llm/glm_client.py. Leave empty to run with deterministic NL templates.
GLM_API_KEY=dbf819611c0847fd9970f7916abd11fe.gQpHR5eEBXmlNNrT

# Prefect Cloud API key (Phase 7 deployment). Used by `prefect cloud login`
# and the prefect library via the PREFECT_API_KEY env var.
PREFECT_API_KEY=pnu_Mcrd12RMZSkFwxw2tJy3nPOfDFMOrS07LRoN
