# Copy to .env and fill in real values. Never commit the real .env file.

# GLM (z.ai) cloud LLM API key — preferred over config.yaml's llm.api_key.
# Read by llm/glm_client.py. Leave empty to run with deterministic NL templates.
GLM_API_KEY=
