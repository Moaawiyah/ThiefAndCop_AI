# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
.venv/

# Per-run logs (regenerated every run; not submission evidence)
artifacts/logs/

# Secrets — never commit keys or OAuth tokens
.env
config.local.yaml
reporting/credentials.json
reporting/token.json

# Assignment brief (not part of the submitted source)
hw6.md
hw6.pdf

# Loose macOS screen-capture files at the repo root (the curated README images
# live under screenshots/ and ARE committed).
Screenshot*.png

# OS
.DS_Store
