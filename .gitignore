# Python caches
__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.coverage
.venv/

# Per-run logs (regenerated every run; not submission evidence)
artifacts/logs/

# Secrets — never commit keys or OAuth tokens
.env
config.local.yaml
reporting/credentials.json
reporting/token.json

# OS
.DS_Store
